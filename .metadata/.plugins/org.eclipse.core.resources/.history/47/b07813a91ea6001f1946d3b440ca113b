package com.Problem_Set_I;

public class Armstrong_number {
	public Integer Find_Armstrong(int n) {
		int orginal=n;
		int sum=0;
		int digits;
		digits=String.valueOf(n).length();
		System.out.println(digits);
		while(n!=0) {
			sum+=sum+(n%10)**digits
		}
		
		return sum;
	}

}
